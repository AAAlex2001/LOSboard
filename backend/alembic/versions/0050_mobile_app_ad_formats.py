"""Добавить секцию «Реклама в мобильном приложении LOS» в pricing-tech.

Revision ID: 0050_mobile_app_ad_formats
Revises: 0049_clean_tour_mockups
Create Date: 2026-07-20 00:00:00.000000

"""
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op


revision: str = "0050_mobile_app_ad_formats"
down_revision: Union[str, None] = "0049_clean_tour_mockups"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


SECTION_TITLE = "Реклама в мобильном приложении LOS"

MOBILE_APP_SECTION = f"""\
<h1>{SECTION_TITLE}</h1>
<h2>1. Баннер (фото или видео)</h2>
<p>[INFO Где показывается]</p>
<p>При входе в приложение</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Видео</strong> — формат MP4 и MOV, вертикальная ориентация (9:16) 720x1280 px, 1080x1920 px. Рекомендуемая продолжительность ролика — 5–15 секунд. Вы можете размещать ролики до 20 секунд, но в этом случае показатели досмотра могут быть низкими. С пропуском: кнопка Skip проявляется после 5 сек. Вес файла — 2–5 мб для 15-секундного ролика. Битрейт: Full HD (1080p): 4–6 Мбит/с; HD (720p): 2,5–4 Мбит/с; SD (480p): 1,5–2,5 Мбит/с. CTA-кнопка («Установить», «Подробнее») располагается внизу экрана.<br><strong>Изображение</strong> — формат JPEG и PNG, рекомендуемый размер — 600x1200, минимальный размер — 300x600, рекомендуем по бокам изображений не располагать важный контент, максимальный вес — 2 мб.<br><strong>Заголовок</strong> — до 30 символов (включительно)<br><strong>Описание</strong> — до 45 символов (включительно)<br><strong>Ссылка</strong> — ссылка/пиксель<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
<h2>2. Стандартный баннер (606x350px) + информационная карточка (606x350px)</h2>
<p>[INFO Где показывается]</p>
<p>В 15 разделах мобильного приложения Тур-гида LOS на выбор (зависит от Вашего предложения)</p>
<p>[/INFO]</p>
<p>[INFO Разделы сайта для размещения рекламы на выбор]</p>
<p>Административные здания; Аптеки; Винодельни; Заправки; Культурные достопримечательности; Магазины и рынки; Мойки; Отели; Парковки; Пляжи; Ремонт одежды и обуви; Рестораны; Салоны красоты; Спортивные залы; Церкви</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Изображение</strong> — формат JPEG и PNG, рекомендуемый размер — 720x750, минимальный размер — 360x375, рекомендуем по бокам изображений не располагать важный контент, максимальный вес — 2 мб<br><strong>Заголовок</strong> — до 30 символов (включительно)<br><strong>Описание</strong> — до 45 символов (включительно)<br><strong>Ссылка</strong> — ссылка/пиксель<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
<h2>3. Универсальный баннер для горных маршрутов (320x260px) (2 варианта)</h2>
<p>[INFO Где показывается]</p>
<p>В разделе сайта «Горные маршруты»</p>
<p>[/INFO]</p>
<p>[INFO Обратите внимание]</p>
<p>Рекламу можно разместить как с фото/логотипом, так и без него (2 варианта выше). Размер фото в карточке: 280x177px</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Изображение/логотип</strong> — формат JPEG и PNG, рекомендуемый размер — 720x750, минимальный размер — 360x375, рекомендуем по бокам изображений не располагать важный контент, максимальный вес — 2 мб<br><strong>Заголовок</strong> — до 30 символов (включительно)<br><strong>Описание</strong> — до 45 символов (включительно)<br><strong>Ссылка</strong> — ссылка/пиксель<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
<h2>4. Универсальный баннер для экскурсионных маршрутов</h2>
<p>[INFO Где показывается]</p>
<p>В разделе сайта «Экскурсии»</p>
<p>[/INFO]</p>
<p>[INFO Обратите внимание]</p>
<p>Рекламу можно разместить с фото или логотипом. Размер фото в карточке: 310x107px</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Изображение/логотип</strong> — формат JPEG и PNG, рекомендуемый размер — 720x750, минимальный размер — 360x375, рекомендуем по бокам изображений не располагать важный контент, максимальный вес — 2 мб<br><strong>Заголовок</strong> — до 30 символов (включительно)<br><strong>Описание</strong> — до 45 символов (включительно)<br><strong>Ссылка</strong> — ссылка/пиксель<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
<h2>5. Баннер для мероприятий (320x131px)</h2>
<p>[INFO Где показывается]</p>
<p>В разделе сайта «Вечеринки и яркие впечатления».</p>
<p>[/INFO]</p>
<p>[INFO Обратите внимание]</p>
<p>Объём карточки зависит от наполнения — минимум 320x362px. Размер баннера 320x131px</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Изображения</strong> — формат JPEG и PNG, рекомендуемый размер — 1800x1332, минимальный размер — 980x666, рекомендуем по бокам изображений не располагать важный контент.<br><strong>Ссылка</strong> — ссылка/пиксель.<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
<h2>6. Универсальный баннер для мобильной связи (320x266px)</h2>
<p>[INFO Где показывается]</p>
<p>В разделе сайта «Интернет и мобильная связь»</p>
<p>[/INFO]</p>
<p>[INFO Обратите внимание]</p>
<p>Рекламу можно разместить с фото или логотипом. Объём карточки зависит от наполнения — минимум 320x240px. Размер фото в карточке 200x50px</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Изображение/логотип</strong> — формат JPEG и PNG, рекомендуемый размер — 720x750, минимальный размер — 360x375, рекомендуем по бокам изображений не располагать важный контент, максимальный вес — 2 мб<br><strong>Заголовок</strong> — до 30 символов (включительно)<br><strong>Описание</strong> — до 45 символов (включительно)<br><strong>Ссылка</strong> — ссылка/пиксель<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
<h2>7. Универсальный баннер для такси (320x217px)</h2>
<p>[INFO Где показывается]</p>
<p>В разделе сайта «Такси»</p>
<p>[/INFO]</p>
<p>[INFO Обратите внимание]</p>
<p>Рекламу можно разместить с фото или логотипом. Объём карточки зависит от наполнения — минимум 320x220px. Размер фото в карточке 290x60px</p>
<p>[/INFO]</p>
<p>[INFO Рекомендации]</p>
<p><strong>Изображение/логотип</strong> — формат JPEG и PNG, рекомендуемый размер — 720x750, минимальный размер — 360x375, рекомендуем по бокам изображений не располагать важный контент, максимальный вес — 2 мб<br><strong>Заголовок</strong> — до 30 символов (включительно)<br><strong>Описание</strong> — до 45 символов (включительно)<br><strong>Ссылка</strong> — ссылка/пиксель<br><strong>Возрастное ограничение</strong> — опционально, например, 6+</p>
<p>[/INFO]</p>
"""


def upgrade() -> None:
    """Добавить секцию один раз, сохранив остальные вкладки аккордеона."""
    conn = op.get_bind()
    row = conn.execute(
        sa.text("SELECT body FROM content_pages WHERE slug = 'pricing-tech'")
    ).first()
    if not row or SECTION_TITLE in row[0]:
        return

    body = row[0].rstrip() + "\n" + MOBILE_APP_SECTION
    conn.execute(
        sa.text(
            "UPDATE content_pages SET body = :body, updated_at = now() "
            "WHERE slug = 'pricing-tech'"
        ),
        {"body": body},
    )


def downgrade() -> None:
    """Удалить добавленную секцию, не затрагивая предшествующий контент."""
    conn = op.get_bind()
    row = conn.execute(
        sa.text("SELECT body FROM content_pages WHERE slug = 'pricing-tech'")
    ).first()
    if not row:
        return

    marker = f"<h1>{SECTION_TITLE}</h1>"
    index = row[0].find(marker)
    if index < 0:
        return

    conn.execute(
        sa.text(
            "UPDATE content_pages SET body = :body, updated_at = now() "
            "WHERE slug = 'pricing-tech'"
        ),
        {"body": row[0][:index].rstrip()},
    )
